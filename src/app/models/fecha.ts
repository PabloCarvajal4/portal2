export class Fechas {

    id: number | undefined;
    year: number | undefined;
    month: number | undefined;

    constructor(id:number, year: number, month: number){
        
        this.id = id;
        this.year = year;
        this.month = month;

    }
}