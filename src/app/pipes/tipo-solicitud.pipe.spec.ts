import { TipoSolicitudPipe } from './tipo-solicitud.pipe';

describe('TipoSolicitudPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoSolicitudPipe();
    expect(pipe).toBeTruthy();
  });
});
