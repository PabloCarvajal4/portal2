import { IncapacidadesPipe } from './incapacidades.pipe';

describe('IncapacidadesPipe', () => {
  it('create an instance', () => {
    const pipe = new IncapacidadesPipe();
    expect(pipe).toBeTruthy();
  });
});
