import { VacacionesPipe } from './vacaciones.pipe';

describe('VacacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new VacacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
