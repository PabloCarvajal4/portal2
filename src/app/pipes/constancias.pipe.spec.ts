import { ConstanciasPipe } from './constancias.pipe';

describe('ConstanciasPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstanciasPipe();
    expect(pipe).toBeTruthy();
  });
});
