import { TipoConstanciaPipe } from './tipo-constancia.pipe';

describe('TipoConstanciaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoConstanciaPipe();
    expect(pipe).toBeTruthy();
  });
});
